import 'package:flutter/material.dart';

class Ec2 extends StatelessWidget {
  const Ec2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Chapter 6 Exercise 2')),
      body: Center(child: Text('Chapter 6 Exercise 2')),
    );
  }
}